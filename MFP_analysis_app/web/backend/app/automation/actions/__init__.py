"""Import action modules so they register with the automation registry."""

from . import (  # noqa: F401
    lcms_eic,
    lcms_export,
    lcms_features,
    lcms_kendrick,
    lcms_polymer,
    lcms_sessions,
    lcms_spectrum,
)
