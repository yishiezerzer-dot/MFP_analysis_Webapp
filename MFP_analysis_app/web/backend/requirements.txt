# MFP Analysis Web — FastAPI backend
# Python 3.10+ recommended.
#
# The server imports shared analysis code from MFP_analysis_app/lab_gui/
# (that folder must be present next to web/ in the same checkout).
#
# These are the direct dependencies. Deployments install the pinned set in
# requirements.lock; regenerate it after editing this file:
#   uv pip compile requirements.txt --universal --python-version 3.12 -o requirements.lock

# --- Web API ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# --- Analysis stack (LCMS, FTIR, Plate Reader, Data Studio) ---
# numpy>=2.0: FTIR integration uses np.trapezoid.
numpy>=2.0,<3
# pandas<3: Data Studio decimal-comma import and ffill break on pandas 3 (see plan.md task 2.3).
pandas>=2.0,<3
pyteomics>=4.7
psims>=1.0
scipy>=1.11
openpyxl>=3.1
xlrd>=2.0
lxml>=5.0

# --- AI assistant (OpenAI + HTTP client for Ollama) ---
httpx>=0.27
openai>=1.40

# --- Publication & Figure Engine ---
reportlab>=4.0
pillow>=10.0
